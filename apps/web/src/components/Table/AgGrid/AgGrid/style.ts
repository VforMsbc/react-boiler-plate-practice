import { SxProps } from "@mui/material";

const gridIcons: SxProps = {
  "&.MuiIconButton-root": {
    padding: "0 !important",
  },
};

const grid: SxProps = {
  width: "100%",
};

export { gridIcons, grid };
